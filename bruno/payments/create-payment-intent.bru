meta {
  name: Create Payment Intent (501)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/bookings/{{booking_id}}/payments/intent
  body: json
  auth: none
}

body:json {
  {
    "type": "full"
  }
}
