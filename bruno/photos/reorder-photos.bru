meta {
  name: Reorder Photos
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/api/v1/properties/{{property_id}}/photos/reorder
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "order": ["{{photo_id}}"]
  }
}
