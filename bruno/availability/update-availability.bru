meta {
  name: Block / Unblock Dates
  type: http
  seq: 2
}

patch {
  url: {{base_url}}/api/v1/properties/{{property_id}}/availability
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "start_date": "2026-08-01",
    "end_date": "2026-08-08",
    "blocked": true,
    "notes": "Maintenance week"
  }
}
